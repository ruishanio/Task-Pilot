<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruishanio.taskpilot.admin.mapper.TaskReportMapper">
	
	<resultMap id="TaskReport" type="com.ruishanio.taskpilot.admin.model.TaskReport" >
		<result column="id" property="id" />
	    <result column="trigger_day" property="triggerDay" />
		<result column="running_count" property="runningCount" />
	    <result column="suc_count" property="sucCount" />
	    <result column="fail_count" property="failCount" />
	</resultMap>

	<sql id="Base_Column_List">
		t.id,
		t.trigger_day,
		t.running_count,
		t.suc_count,
		t.fail_count
	</sql>
	
	<insert id="saveOrUpdate" parameterType="com.ruishanio.taskpilot.admin.model.TaskReport" useGeneratedKeys="true" keyProperty="id" >
		INSERT INTO task_report (
			trigger_day,
			running_count,
			suc_count,
			fail_count
		) VALUES (
			 #{triggerDay},
			 #{runningCount},
			 #{sucCount},
			 #{failCount}
		 )
		<!-- PostgreSQL 通过冲突更新日报聚合，避免并发场景下重复插入。 -->
		ON CONFLICT (trigger_day) DO UPDATE
		SET running_count = EXCLUDED.running_count,
			suc_count = EXCLUDED.suc_count,
			fail_count = EXCLUDED.fail_count
	</insert>

	<select id="queryLogReport" resultMap="TaskReport">
		SELECT <include refid="Base_Column_List" />
		FROM task_report AS t
		WHERE t.trigger_day between #{triggerDayFrom} and #{triggerDayTo}
		ORDER BY t.trigger_day ASC
	</select>

	<select id="queryLogReportTotal" resultMap="TaskReport">
		SELECT
			SUM(running_count) running_count,
			SUM(suc_count) suc_count,
			SUM(fail_count) fail_count
		FROM task_report AS t
	</select>

</mapper>
